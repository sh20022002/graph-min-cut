const fs = require('fs');

fs.readFile('javaScript\\graphCut\\graph.txt', 'utf-8', (err, data) => {
    if(err){
        console.error(err);
        return;
    }

});

function minCut(graph){
    while(v > 2){
        
    }
}